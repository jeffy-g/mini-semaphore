{
  "name": "mini-semaphore",
  "version": "1.1.10",
  "description": "A lightweight version of Semaphore",
  "private": false,
  "main": "./cjs/index.js",
  "module": "./esm/index.js",
  "unpkg": "./umd/index.js",
  "sideEffects": false,
  "types": "./index.d.ts",
  "typings": "./index.d.ts",
  "author": "jeffy-g",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/jeffy-g/mini-semaphore/issues"
  },
  "defs": {
    "regex": "/,\\s?\\w\\.t\\s?=[^]+(?=,\\s?i\\.o)/"
  },
  "homepage": "https://github.com/jeffy-g/mini-semaphore#readme",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/jeffy-g/mini-semaphore.git"
  },
  "engines": {
    "node": ">=10",
    "yarn": "^1.22.4"
  },
  "files": [
    "package.json",
    "README.md",
    "LICENSE",
    "cjs",
    "esm",
    "umd",
    "webpack",
    "*.d.ts"
  ],
  "keywords": [
    "lock",
    "mutex",
    "promise",
    "semaphore",
    "concurrency",
    "javascript",
    "typescript"
  ],
  "scripts": {
    "all": "yarn clean && yarn lint && yarn dist && yarn webpack && yarn copy:types",
    "build": "tsc",
    "build:esm": "tsc -p src/tsconfig.json && node ./scripts/tools -cmd rmc -basePath ./build/esm -test \"/.js$/\"",
    "clean": "rimraf build/* dist/* logs/*.tsbuildinfo",
    "copy:types": "cpx -v src/index.d.ts dist && cpx -v src/index.d.ts dist/cjs && cpx -v src/index.d.ts dist/esm && cpx -v src/index.d.ts dist/umd && cpx -v src/index.d.ts dist/webpack",
    "dist": "rimraf \"dist/!(webpack)\" && yarn build && yarn build:esm && cpx -v \"./{package.json,README.md,LICENSE}\" dist && cpx -v \"./build/**/*.js\" dist",
    "inst:clean": "rimraf yarn.lock node_modules && yarn install",
    "lint": "eslint src/**/*.ts",
    "test": "jest --coverage --silent=false -c=jest.config.js",
    "update:packages": "yarn upgrade-interactive --latest",
    "webpack": "rimraf ./dist/webpack/* ./dist/umd/* && npx webpack && yarn webpack:strip && cpx -v \"./dist/*.d.ts\" ./dist/webpack/",
    "webpack:strip": "node ./scripts/tools -cmd stripWebpack -regex \"%npm_package_defs_regex%\""
  },
  "devDependencies": {
    "@types/jest": "^25.2.1",
    "@types/node-fetch": "^2.5.7",
    "@types/webpack": "^4.41.12",
    "@typescript-eslint/eslint-plugin": "^2.33.0",
    "@typescript-eslint/eslint-plugin-tslint": "^2.33.0",
    "@typescript-eslint/parser": "^2.33.0",
    "cpx": "latest",
    "eslint": "^7.0.0",
    "jest": "^26.0.1",
    "node-fetch": "^2.6.0",
    "rimraf": "^3.0.2",
    "rm-cstyle-cmts": "2.4.0",
    "terser-webpack-plugin": "^3.0.1",
    "ts-jest": "^25.5.1",
    "ts-loader": "^7.0.4",
    "tslint": "^6.1.2",
    "typescript": "^3.9.2",
    "webpack": "^4.43.0",
    "webpack-cli": "^3.3.11"
  }
}
